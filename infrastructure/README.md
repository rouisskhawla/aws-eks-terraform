# Infrastructure

Terraform configuration for the AWS infrastructure.

This folder manages:

* ECR
* AWS infrastructure such as VPC and EKS

GitHub Actions authentication resources are managed separately in `github-actions-oidc/`.
